image: hugomods/hugo:exts-0.166.0

# Deployment is handled by Vercel's own GitLab integration (see vercel.json).
# This pipeline is just a build sanity check on every push/MR.

test:
  stage: test
  script:
    - hugo --gc --minify
  artifacts:
    paths:
      - public
    expire_in: 1 day
